{
  "name": "ai-resume-customizer",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start:background": "node run-in-background.js",
    "stop:background": "node -e \"import { readFileSync } from 'fs'; const pid = Number(readFileSync('server.pid','utf8')); try{ process.kill(pid); console.log('Stopped PID', pid);}catch(e){ console.error('Failed to stop', e.message);} \""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2"
  }
}
